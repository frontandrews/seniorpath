{
  "id": "node-runtime-core",
  "title": "Node Runtime Core",
  "topic": "node",
  "description": "Node runtime and architecture questions focused on event loop, workers, and I/O.",
  "difficulty": "medium",
  "estimatedMinutes": 8,
  "tags": ["node", "runtime", "event-loop"],
  "cards": [
    {
      "id": "node-single-threaded-meaning",
      "type": "concept",
      "difficulty": "medium",
      "question": "Is Node.js single-threaded?",
      "shortAnswer": "The JavaScript execution model is centered on a single-threaded event loop, but Node itself is not limited to one thread overall.",
      "expectedAnswer": "Node.js runs JavaScript primarily on a single-threaded event loop, but the runtime is not limited to one thread overall. It uses libuv, can offload some work to a thread pool, and can use worker threads for CPU-bound tasks. So the right answer is that the main JS execution path is single-threaded, but the runtime can still use multiple threads.",
      "keyPoints": [
        "main JS execution is single-threaded",
        "runtime can use thread pool or workers",
        "distinguish JS execution from the whole runtime"
      ],
      "learnMore": "The phrase 'Node is single-threaded' is only partly true. It is useful as shorthand for the main JavaScript execution path, but it becomes misleading when people forget about libuv, the thread pool, and worker threads.\n\nA strong interview answer separates the event loop from the runtime. That shows you understand why Node handles lots of I/O well, yet can still suffer badly when CPU-heavy work blocks the main thread.",
      "learnMoreSlug": "node-single-threaded-does-not-mean-what-people-think",
      "exampleCode": "import { Worker } from 'node:worker_threads'\n\nfunction runHeavyJob() {\n  return new Worker(new URL('./worker.js', import.meta.url))\n}\n\n// main request handling stays responsive while CPU work moves away\nrunHeavyJob()",
      "exampleLanguage": "ts",
      "commonTraps": [
        "answering simply yes or no without nuance",
        "assuming async automatically means multi-threaded execution"
      ],
      "followUps": [
        "When would you use worker threads?",
        "What kind of work blocks the event loop?"
      ],
      "tags": ["node", "concurrency", "workers"]
    },
    {
      "id": "node-worker-vs-cluster",
      "type": "tradeoff",
      "difficulty": "medium",
      "question": "What is the difference between worker threads and cluster in Node.js?",
      "shortAnswer": "Worker threads help with CPU-bound work inside one process, while cluster runs multiple Node processes to use multiple cores and isolate failures more cleanly.",
      "expectedAnswer": "Worker threads are useful when you need CPU-bound parallelism inside a process and want to offload heavy computation from the main event loop. Cluster is about running multiple Node processes, often to use multiple CPU cores for request handling and get stronger process isolation. They solve related but different problems.",
      "keyPoints": [
        "worker threads for CPU-bound work",
        "cluster for multiple processes and core usage",
        "different tradeoffs around isolation and communication"
      ],
      "learnMore": "Worker threads are about parallel computation inside one process. Cluster is about process-level scaling and isolation. That means the communication model, memory boundaries, and failure behavior are different.\n\nThe clean interview framing is: worker threads are usually about keeping CPU-heavy work off the main event loop, while cluster is usually about scaling request handling across cores with stronger isolation.",
      "exampleCode": "import cluster from 'node:cluster'\nimport { Worker } from 'node:worker_threads'\n\nif (cluster.isPrimary) {\n  cluster.fork()\n}\n\nconst hashWorker = new Worker(new URL('./hash-worker.js', import.meta.url))",
      "exampleLanguage": "ts",
      "commonTraps": [
        "treating them as interchangeable",
        "ignoring process isolation differences"
      ],
      "followUps": [
        "What is the communication cost with workers?",
        "When would horizontal scaling be simpler?"
      ],
      "tags": ["node", "worker-threads", "cluster"]
    }
  ]
}
