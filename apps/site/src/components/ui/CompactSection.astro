---
import { cn } from '@/lib/cn'
import { ui } from '@/lib/ui'

type Props = {
  className?: string
}

const { className = '', ...rest } = Astro.props
---

<section {...rest} class={cn(ui.compactSection, className)}>
  <slot />
</section>
